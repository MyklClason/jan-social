<% locals = local_assigns %>
<% th = "tables/th" %>
<thead>
  <tr>
    <%= (render partial: th, locals: {body: 'Full Name' }) %>
    <%= (render partial: th, locals: {body: 'Follow' }) if locals[:display_voting] %>
    <%= (render partial: th, locals: {body: 'Following' }) if locals[:display_voting] %>
    <%= (render partial: th, locals: {body: 'Followers' }) if locals[:display_voting] %>
    <%= (render partial: th, locals: {body: 'Actions', colspan: "3" }) if locals[:display_actions] %>
  </tr>
</thead>