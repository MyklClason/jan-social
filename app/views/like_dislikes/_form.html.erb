<%= form_for(@like_dislike) do |f| %>
  <% if @like_dislike.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@like_dislike.errors.count, "error") %> prohibited this like_dislike from being saved:</h2>

      <ul>
      <% @like_dislike.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :likes %><br>
    <%= f.check_box :likes %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
